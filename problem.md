# AI 데이터 분석: 데이터 기반 트렌드 분석

> 코디세이 `AI 응용 학습 (AI Native Master)` 과정 · `codyssey mission` API로 원문 그대로 발췌.

### 1. 미션 소개

그래프를 그리는 건 어렵지 않습니다. 어려운 건 그게 무슨 의미인지 해석하는 일입니다.

숫자가 올라가는 이유, 특정 시점에 꺾이는 이유 — 데이터는 스스로 말하지 않습니다. 읽어내야 합니다.

시계열 데이터에서 패턴을 발견하고 "그래서 이게 무슨 의미인가"를 스스로 해석하는 과정을 경험합니다.

데이터는 그 자체로는 의미가 없습니다. 데이터를 정제하고, 시각화하고, 패턴을 발견해야 비로소 의사결정에 활용할 수 있는 인사이트가 됩니다.

주식 가격의 흐름, 기온 변화, 판매량 추이처럼 시간에 따라 변화하는 데이터를 시계열 데이터라고 합니다.

시계열 분석을 통해 과거의 패턴을 파악하고, 미래를 예측하며, 이상 징후를 감지할 수 있습니다.

이 미션에서는 시계열 데이터를 분석하고 인사이트 리포트를 작성합니다.

어떤 데이터를 분석할지, 어떤 질문을 던질지, 결과를 어떻게 해석할지에 집중합니다.

단순히 그래프를 그리는 것이 아니라, 데이터에서 의미 있는 패턴을 발견하고 "그래서 이게 무슨 의미인가?"를 스스로 해석하는 과정을 경험합니다.

이 경험은 데이터 기반 의사결정의 기초가 되며, AI를 활용한 분석 역량을 기르는 데 핵심이 됩니다.

### 2. 최종 결과물

다음 조건을 만족하는 분석 결과물 1개를 완성한다.

1. 분석 리포트 (Markdown) — 선택한 시계열 데이터(최소 100개 이상 데이터 포인트), REPORT.md에 분석 주제, 질문, 데이터 설명, 시각화, 인사이트, 결론/한계점을 포함
2. 시각화 결과물 (리포트 내 포함) — 필수 2개 이상, 권장 3개 이상, 이미지 파일 또는 노트북 출력 캡처를 리포트에 포함(링크 정상 동작)
3. Python 코드 — Jupyter Notebook(.ipynb) 또는 Python 스크립트(.py)
4. GitHub 저장소 — 코드, 리포트, (가능한 범위 내) 데이터 또는 데이터 수집 방법 문서

### 3. 과제 목표

1. 데이터 분석 사고 — 질문 3개 이상 정의, 결측치/이상치 처리 기준 설명, 관찰과 해석 구분
2. 시계열 데이터 이해 — 트렌드/계절성/노이즈 개념, 이동평균/변화율 등 기법 적용 이유
3. AI 활용 역량 — AI에게 질문을 던지고 생성된 코드/해석을 검증, 최종 판단은 본인이 근거를 들어 설명

### 4. 기능 요구 사항

1. **분석 주제 및 데이터 선정**: 관심 있는 시계열 데이터 1개(주식/암호화폐/기온/판매량/환율/조회수 등), 최소 100개 이상 데이터 포인트, 출처와 기간 명시
2. **분석 질문 설정**: 답하고 싶은 질문 3개 이상 정의
3. **데이터 정제 및 시계열 분석**: 기본 정보(기간/컬럼/결측치) 확인, 결측치/이상치 기준 정해 처리, 시계열 분석 기법 최소 2가지(이동평균/변화율/구간별통계/변동성/월별요일별집계 등)
4. **시각화**: 필수 2개 이상, 권장 3개 이상
5. **인사이트 도출 및 리포트 작성**: 인사이트 3개 이상, 각각 "그래프에서 확인한 관찰(수치/근거)" 포함, REPORT.md에 정리

### 5. 보너스 과제

1. **분석 결과 서비스화(대시보드/웹)**: 기간/조건을 바꿔보며 탐색 가능하게 구성. 제출은 (1)배포 URL (2)로컬 실행 화면 녹화+실행방법 문서 (3)대시보드 스크린샷 세트+필터/기간 변경 시나리오 설명 중 택1.
2. **시계열 심화 옵션(분해/예측)**: (A)시계열 분해(추세/계절성 분리) 또는 (B)간단 예측(베이스라인 방식, 정확도보다 가정/한계 설명 집중) 중 1개 이상, 시각화 1개 이상 포함.
